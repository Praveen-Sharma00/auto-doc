#!/usr/bin/env node
import('../dist/cli.js')
  .then(({ main }) => main())
  .then(
    (code) => process.exit(code ?? 0),
    (err) => {
      console.error(err instanceof Error ? (err.stack ?? err.message) : err);
      process.exit(1);
    },
  );
